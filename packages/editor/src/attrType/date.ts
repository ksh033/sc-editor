import { VdProFormColumnsType } from '../interface/index';

const date: VdProFormColumnsType = {
  title: '日期',
  dataIndex: 'date',
  valueType: 'date',
  fieldProps: {
    style: { width: '100%' },
  },
};
export default date;
